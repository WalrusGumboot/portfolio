import * as React from "react"
import Frame from "../components/frame.js"

const NotFoundPage = () => (
  <Frame pageTitle="404: page not found">
    <p>You just hit a route that doesn't exist... the sadness.</p>
  </Frame>
)

export default NotFoundPage
